# Copy to .env to run Position Watch on your own computer. Never commit .env.
# In GitHub, the same names are repository secrets (Settings > Secrets and variables > Actions).

FMP_API_KEY=
FINNHUB_API_KEY=
ANTHROPIC_API_KEY=
MAIL_USER=
MAIL_APP_PASSWORD=
DASHBOARD_PASSCODE=
